# Project Rules for Claude Code

## Permissions

- Claude has autonomous permission to use Bash, Edit, Write, Read, and MCP (Playwright, etc.) tools without asking for confirmation.

## Git Workflow

- After completing a significant change, always ask the user whether to `git commit` and `git push`.
- Do NOT commit or push automatically — wait for explicit user approval.
